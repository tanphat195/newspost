import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  main: {
    width: '100%',
    height: 15,
    borderBottomWidth: 1,
    borderColor: 'rgba(0,0,0,0.05)',
    marginBottom: 15,
  },
});